'use strict';

const AWS = require('aws-sdk'); // eslint-disable-line import/no-extraneous-dependencies

const dynamoDb = new AWS.DynamoDB.DocumentClient();

module.exports.character = (event, context, callback) => {
  const params = {
    TableName: process.env.DYNAMODB_TABLE_CHARACTERS,
    Key: {
      character_id: event.pathParameters.id,
    },
  };

  console.log(`fetching the GoT character: ${event.pathParameters.id}`)

  // fetch todo from the database
  dynamoDb.get(params, (error, result) => {
    // handle potential errors
    if (error) {
      console.error(error);
      callback(null, {
        statusCode: error.statusCode || 501,
        headers: { 'Content-Type': 'text/plain' },
        body: 'Couldn\'t fetch the character item. ' + error,
      });
      return;
    }

    // create a response
    const response = {
      statusCode: 200,
      body: JSON.stringify(result.Item),
    };
    callback(null, response);
  });
};
